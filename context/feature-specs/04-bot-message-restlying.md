it seems the message the llm returns back is in a form of markdown cause i see "**" for bold text. let's implement the bot message to use markdown from opentui?

Error:
- i noticed something as well, The markdown is only truly visible when I leave and open and the session again, but as it’s rendering/streaming it doesn’t show the markdown immediately 