now that we've started extending the command menu i want to add more commands that'll trigger actions, the following:
- Slash commands/skills
    - /context (show context window)
    - /compact (manual compacting)
    - /new or /clear(new conversation)
    - /voice (to toggle speech to text)
    - /handoff (to start a new session with summary from previous session)

things like this, but we will have to tackle each one by one since they all seem to be a feature spec on their own. we will need to see how we will order them to go from the easiest to the most complex. i still have a feature spec which focuses on context management and compacting, should we remove them from here and wait till we start on that feature spec?
