once a user submits a message the input bar gets disabled so the user has to monitor the agent to see when it is done before it can send the next message they want to send. i want the user to be able to send as many messages as they want even while the agent is streaming the messages just get sent to a queue and immediately the agent has finished streaming or is done with the response that the queue starts offloading one by one until the queue is empty or the user decides to stop the agent.

and if the user does not want to wait for the agent to finish before it processes the message, for instance the agent may be drifting from what it needs to do the user can just hit enter again to send the next message in the queue, it'll interrupt the agent then force send the next message to the agent