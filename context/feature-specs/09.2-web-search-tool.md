here i want to implement a web search tool that the agent can use to search the web and get relevant information to solve problems and implement solutions

in a tutorial i watched i saw them use "DuckDuckGo" search api, but it was in a python project and i don't know how to implement it in a typescript project (ensure it is free), if it does not exist you can use the next best thing.

## Rules
for this task i need you to create an **isolated git worktree** to work on this feature so it doesn't conflict with other things or cause any bugs in the current codebase.

we can have as input a query, and max result field, defaults to 10, ge 1, le 20.

so it'll return search results with url, title and snippet.