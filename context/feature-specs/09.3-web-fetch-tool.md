here i want to implement a web fetch tool that the agent can use to fetch content from a url.

i don't know what tool we can use to perform the web fetch, you can decide for yourself.

the tool should have a url as input as well as a timeout (default 30s, ge 5s le 120s) and return the response body as text.

you can add that we truncate the text if it is too long (the length depends on what you feel is appropriate)

## Rules
for this task i need you to create an **isolated git worktree** to work on this feature so it doesn't conflict with other things or cause any bugs in the current codebase.