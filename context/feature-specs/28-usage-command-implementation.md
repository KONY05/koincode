i was thinking if we could add a usage command for users to easily see their usage of their api keys, but in this implementation we don't handle it we just open the browser to the link of their current model their using, like the provider, so if i'm using an anthropic model, and the anthropic key is present we send them to whatever anthropic usage page they have, same goes for openai, gemini, and openrouter, but the thing  is that  we also need to manage edge cases, where if an anthropic model is set up, but anthropic key is not present we check the key that's present and send them there, i'm sure there are more cases i can't think of at the moment. there's also a case where by someone uses openrouter key for frontier models and so on, things like that