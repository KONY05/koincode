i want us to check how open code handle their theme rendering, cause we had errors in rendering the ui in a normal terminal, the colors weren't being applied only in vscode terminals, let's see how they got their to work. and if we can implement the same.

link to opencode github:
https://github.com/anomalyco/opencode/tree/dev/packages/opencode