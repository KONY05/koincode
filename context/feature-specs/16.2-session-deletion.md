so we just implemented message deletion using the "esc esc" command to delete the last/most recent message, now let's implement deleting for a session, how will that work? what command can we use for this? this will be when the session-dialog is up. should we add a confirm? or an undo in the toast?