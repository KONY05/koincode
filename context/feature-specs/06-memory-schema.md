now i want us to implement the memory table and use it to store user memory, the database table should be in the prisma database, we should be able to add, update and delete user memory, the user memory should be in the system prompt when we are building the system prompt.

as a later part of this we will add a tool call that does the addition, updating and deletion and listing of user memory, so that means we will have to have routes for all of these now, only the adding it as a tools is subjected as a later task, add it to the progress tracker.
