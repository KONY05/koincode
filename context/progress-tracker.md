# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Phase 2: Local-First Pivot — planning

## Current Goal

- Phase 2 Feature 01: Strip auth & billing, switch to SQLite, add OpenRouter + direct provider key support

## Completed

### Phase 1 — Original Tutorial Build (nightcode → koincode)

- Feature 01: Project setup and component architecture — Bun monorepo scaffolded with four workspaces (`@koincode/cli`, `@koincode/server`, `@koincode/database`, `@koincode/shared`). Root `package.json` with `dev:cli`, `dev:server`, `build:cli`, `link:cli` scripts. `tsconfig.base.json` with strict mode. CLI entry point `packages/cli/src/index.tsx` bootstrapping OpenTUI renderer.
- Feature 02: UI infrastructure — OpenTUI and React 19 wired up. Terminal layout primitives (Box, Text) confirmed working. Theme and provider scaffolding in place.
- Feature 03: Routing and screen layout — React Router 7 installed. Three screens added: `Home` (session list), `NewSession`, `Session` (active chat). Screen shell layouts and navigation flow confirmed working in terminal.
- Feature 04: Server, shared, and database packages — Hono server at `packages/server/src/index.ts` on port 3000. Route groups `/auth`, `/billing`, `/sessions`, `/chat` scaffolded. Prisma schema with `Session` model (id, userId, title, messages JSON, timestamps). `@koincode/shared` with model registry (`models.ts`) and Zod tool schemas (`schemas.ts`). Prisma client generated via `@prisma/adapter-pg`.
- Feature 05: AI chat streaming — `POST /chat/submit` wired to AI SDK (`ai` + `@ai-sdk/anthropic`). Server streams text generation back to CLI. `hooks/use-chat.ts` in CLI consumes the stream via `@ai-sdk/react`. Streaming responses render in real time in the Session screen.
- Feature 06: Session management and config — Session CRUD endpoints (`GET /sessions`, `POST /sessions`, `GET /sessions/:id`, `PATCH /sessions/:id`). Session list on Home screen. Session title auto-generated from first message. Full message history persisted as JSON blob on the `Session` model. Model selection config wired.
- Feature 07: Tool calling — AI SDK tool calling enabled on server. PLAN mode tool set (readFile, listDirectory, glob, grep) and BUILD mode tool set (+ writeFile, editFile, bash) defined in `@koincode/shared/schemas.ts`. Tool calls streamed from server to CLI for local execution. Tool results sent back to server for next model turn.
- Feature 08: User experience — Terminal UI polish: command menu, keyboard shortcuts, toast notifications, dialog provider, prompt config provider. Mode indicator (PLAN/BUILD) visible in the session screen. Smooth streaming display and input bar improvements.
- Feature 09: Billing — Polar SDK integration. `credits.ts` middleware checks Polar credit balance before each chat request. Post-response credit ingestion from token usage. `/billing/checkout` route opens Polar checkout flow.
- Feature 10: Client-side tool execution — Tool calls fully executed on the CLI side (not server-side). Server streams tool call definitions; CLI runs them locally against the user's filesystem. Tool results returned to server to continue the AI turn. Filesystem isolation confirmed — server never touches user files.
- Feature 11: Final polish — End-to-end flow verified. Readme expanded with setup guide and branch-based tutorial structure. CodeRabbit badge added.
- Rebrand: nightcode → koincode — Package names, imports, CLI binary, and all references updated from `@nightcode/*` to `@koincode/*`. Branch: `refactor/rebrand-to-koincode`.

## In Progress

- **NPM binary distribution (Feature 33)** — Distribute compiled standalone binaries through npm so `npm i -g koincode` installs a native binary with no Bun/Node runtime needed at execution time. Platform-specific npm packages (`koincode-darwin-arm64`, `koincode-darwin-x64`, `koincode-linux-x64`, `koincode-linux-arm64`, `koincode-windows-x64`) published alongside the main `koincode` package via `optionalDependencies`. Thin Node wrapper (`bin/npm-wrapper.js`) resolves the correct platform binary with signal forwarding; falls back to Bun JS bundle for unsupported platforms. Self-update for curl/iex installs: background binary download + replace, with permission-denied fallback. `--update` CLI flag for manual updates. CI workflow updated to publish platform packages before main package. Spec: `context/feature-specs/33-npm-binary-distribution.md`.

## Recently Completed (Phase 2 — continued)

- **KOINCODE-Review integration, phase 1 (Feature 42)** — Five new flat, hyphenated commands (this codebase's command menu has no subcommand parsing, so the original `/review connect`-style draft was corrected during design): `/review-login`, `/review-connect`, `/review-disconnect`, `/review-status`, `/review-open`. Bridges the CLI to the separate KOINCODE-Review product (a hosted PR-review SaaS) via a browser-based device-authorization flow — entirely opt-in, no changes to the local server/database, no auth added to KOINCODE itself. `lib/review-auth.ts` reads/writes a new `~/.koincode/review-auth.json` (new `REVIEW_AUTH_FILE` path in `@koincode/shared`) — deliberately kept separate from `~/.koincode/config.json`/`KoincodeGlobalConfig`, since it holds a bearer token for an external service rather than a model provider key. `lib/review-api.ts` is a plain-`fetch` typed client (no HTTP library — matches `api-client.ts`'s existing convention), with `reviewApiUrl` resolved via the same `NODE_ENV === "production"` gate already used in `lib/sentry.ts`/`lib/analytics.ts` (prod: `https://koincode-review.vercel.app`, dev: `http://localhost:3000`, no config field or env var override). `lib/review-repo.ts`'s `resolveCurrentRepo()` parses the current directory's `git remote get-url origin` (same `execSync` pattern as `lib/git-status.ts`) into a GitHub owner/repo pair, handling both SSH and HTTPS remote URL forms. `/review-status` opens a dialog (`components/dialogs/review-status-dialog.tsx`, `ReviewStatusDialogContent`) rather than a toast, matching `/context`'s pattern. `/review-open` sends the browser to the Review dashboard's `/repos` list unconditionally — no dedicated per-repo page exists on the Review side yet, and hitting `/repos` while logged out already redirects to the Review landing/login page via its own Clerk middleware, so no separate logged-out branch was needed in the CLI. `/review-login` runs a poll loop (interval/expiry from the server's response) against the Review API's device-auth endpoints; a 401 from any authed call clears the stale `review-auth.json` so the next command fails fast with a "run /review-login" message instead of looping on a dead token. Verified via `bun run typecheck` (clean across all four packages); **not yet verified live** — no real KOINCODE-Review deployment has run the migration for the API side yet (see that repo's progress tracker), so the device-auth round trip, repo connect/disconnect, and the status dialog have not been exercised end to end. Key/model sync (pushing the CLI's active provider key + model into Review's account settings) remains explicitly deferred to a follow-up phase. Cross-repo API spec: `KOINCODE-Review/context/feature-spec/14-cli-integration-auth.md`. Spec: `context/feature-specs/42-koincode-review-integration.md`.

- **`scheduleWakeup` tool + background sub-agent execution (Features 40, 13 follow-up)** — An initial pass on the wakeup-tool spec designed a full admin-configured recurring cron scheduler (OS-level launchd/cron/`schtasks`, a `ScheduledTask` DB model, permission-ledger gating for unattended runs) before the actual inspiration surfaced: a `ScheduleWakeup`-style tool used *inside an already-running task* to defer its own next check-in, not a recurring job. That collapsed almost all the unattended-execution complexity — since the continuation always resumes a session that's already interactively open, the existing approval widget and tool pipeline apply unchanged.
  - **`scheduleWakeup`** (`packages/shared/src/schemas.ts`, BUILD mode only): `{ delaySeconds (10–1800), reason, prompt }`. `reason` is the short label shown in the transcript; `prompt` is the exact continuation instruction the model writes for its future self, replayed verbatim on fire (not a generic placeholder). Dispatched specially in `use-chat.ts`'s `onToolCall` (like `spawnAgent`/`askUser`/`switchMode`, bypassing `executeLocalTool`): a module-level `_pendingWakeups` map (sessionId → timeout handle, alongside the existing `_activeModes` map) registers a `setTimeout`; firing always pushes onto the existing message queue (`setMessageQueue`) rather than calling `chat.sendMessage`/reading `chat.status` directly — that closure is captured at schedule time and can be many renders stale by fire time (up to 30 min later), whereas `setMessageQueue` is a plain state setter safe to call regardless of closure age, and the pre-existing auto-drain effect already sends queued messages once `chat.status` is actually `"ready"`. Cancelled (cleared) on session unmount and at the top of `submit()` — a real user message always supersedes a pending wakeup. Inline transcript rendering added to `bot-message.tsx`.
  - **Known, accepted limitation**: process-lifetime only — survives the CLI being backgrounded/minimized/lid-closed-without-sleep, not a full quit or machine shutdown. No OS scheduler integration, no DB persistence, deliberately — same posture as other scoped-limitation decisions in this codebase (e.g. the VS Code notification fallback).
  - **Background sub-agent execution** (`spawnAgent`'s `runInBackground` flag, follow-up to Feature 13): `runSpawnAgent` was synchronous-only before this — the parent turn `await`ed the whole sub-agent step loop inline. New in-memory registry `packages/cli/src/lib/agent-background-tasks.ts` (`createAgentBackgroundTask`/`completeAgentBackgroundTask`/`failAgentBackgroundTask`/`getAgentBackgroundTask` — renamed from an initial `background-tasks.ts`/`BackgroundTask` naming, to disambiguate from `session-background-work.ts`'s more generic cancellation registry, see below) tracks task state; when `runInBackground: true`, `use-chat.ts` kicks off `runSpawnAgent(...)` without `await`ing it, returning `{ taskId, status: "running" }` immediately, with `.then`/`.catch` updating the registry on completion. New `checkAgentTask` tool (`packages/cli/src/tools/check-agent-task.ts`, dispatched normally through `executeLocalTool` since it's pure/synchronous, no live-hook dependency) polls the registry by `taskId`. `bot-message.tsx`'s `spawnAgent` view shows `— running in background (task <id>)` when applicable.
  - **System prompt** (`packages/server/src/prompts/system-prompt.ts`): BUILD-mode rules gained a rule pairing `run_in_background`/`runInBackground` with `scheduleWakeup`, so the model knows to defer its check-in instead of polling in the same turn.
  - **Follow-up: event-driven pairing with background task completion.** Checking the reference trace that inspired this feature showed a gap — a background task finishing should be able to resume the session on its own, not only via a pre-scheduled timer that might fire too early (or, worse, never get checked again if the model forgets). Added optional `waitingOnTaskId` to `scheduleWakeup`'s schema. `agent-background-tasks.ts` gained `onAgentTaskSettled(taskId, listener)` — fires once, either on the next microtask if the task already settled before subscribing (closes the race), or from `completeAgentBackgroundTask`/`failAgentBackgroundTask` when it does. `_pendingWakeups`'s map value became `{ timeoutId, unsubscribe? }`; whichever trigger wins first tears down the other so a wakeup only ever fires once — the timer firing calls the stored `unsubscribe()` before queuing the plain `prompt`; the task-settle listener calls `clearTimeout` before queuing `prompt` with the task's result appended (`"${prompt}\n\n---\nTask <id> completed. Result:\n<result>"`, or the error equivalent). Neither path cancels the background task itself — only which trigger is still "live" for that particular wakeup. `bot-message.tsx`'s `scheduleWakeup` view shows `, or sooner if task <shortId> finishes first` when linked; system prompt rule 7 tells the model to pass `waitingOnTaskId` when the wakeup is specifically about a `runInBackground` sub-agent.
  - **Follow-up: cancel background work on session unmount.** Previously nothing ever cancelled a `runInBackground` sub-agent or a `run_in_background` shell process — either kept running unattended (burning tokens/resources) even after the user navigated away or redirected the conversation entirely. New `packages/cli/src/lib/session-background-work.ts` (`registerBackgroundWork(sessionId, cancelFn)` / `cancelAllBackgroundWork(sessionId)`) tracks cancel callbacks per session. `runSpawnAgent` (`spawn-agent.ts`) gained an optional `signal?: AbortSignal`, checked at the top of each step loop iteration and passed into the `fetchWithRestart` call so an in-flight request aborts too; `use-chat.ts`'s background-spawn branch creates an `AbortController`, registers `() => controller.abort()`, and deregisters in a `.finally()` alongside the existing settle handling. `runShellCommand` (`shell.ts`) now takes an optional `sessionId` (threaded from `executeLocalTool`, same pattern as the browser tools), registers `() => proc.kill()` for backgrounded processes, and deregisters via `proc.exited.then(deregister)` so the registry doesn't accumulate entries for long-finished processes. The existing session-unmount cleanup effect (already clearing `_activeModes`/pending wakeups) now also calls `cancelAllBackgroundWork(sessionId)`.
  - **Follow-up: default delivery — `scheduleWakeup` made genuinely optional.** Previously a `runInBackground` result only ever reached the parent if the model explicitly paired it with `scheduleWakeup({ waitingOnTaskId })` — if it didn't (and it's under no obligation to; `scheduleWakeup` was only ever meant as a nice-to-have, not a required pairing), the result sat unseen in the registry, and worse, the new cancel-on-unmount behavior above would actively abort it if the session closed first. Fixed by making delivery unconditional: the moment a background task is created, `use-chat.ts` immediately subscribes via `onAgentTaskSettled` with a default listener that pushes the result straight onto the message queue, via a newly-extracted shared `queueMessage` helper (same "always go through `setMessageQueue`, never call `chat.sendMessage` from a possibly-stale callback" reasoning `scheduleWakeup` already used). Tracked in a new module-level `_defaultTaskListeners` map (taskId → unsubscribe). If the model *also* calls `scheduleWakeup({ waitingOnTaskId })` for that same task, that handler cancels the default listener first, so the task only ever delivers once. Tool descriptions and the system prompt rule reworded to frame `scheduleWakeup` as optional rather than required for background work to reach the parent at all.
  - **Follow-up: more precise sub-agent output + honest partial-progress fallbacks.** The prior "be concise and direct" instruction was replaced with a concrete shape (`finalOutputInstructions` in `spawn-agent.ts`, shared by both prompt-wrapping branches): one-sentence outcome, key findings/changes as actionable bullets, then blockers/uncertainty — explicitly allowed to skip inapplicable sections rather than padding with empty headers. Also fixed: timeout and max-steps previously discarded all of a sub-agent's actual work in favor of a placeholder string, or (for max-steps) whatever text was attached to the very last turn — which by definition still had pending tool calls, so was often a fragment like "let me check X next." New `collectPartialProgress(messages)` gathers text across *all* assistant turns instead; both fallback paths now return that accumulated progress, clearly marked as incomplete, falling back to a placeholder only if there's genuinely nothing to show.
  - **Follow-up: exposed `maxTurns`/`timeoutSeconds` — the timeout path had been unreachable.** Both existed on the internal `SpawnAgentInput` type (set only by the commented-out `CODE_REVIEWER` built-in) but were never in `spawnAgent`'s actual Zod schema or passed through from `use-chat.ts` — every live sub-agent ran with `deadline = null` (no timeout, ever) and a hardcoded `maxSteps = 50`. Added both to `toolInputSchemas.spawnAgent`: `maxTurns` (1–50, default 20) and `timeoutSeconds` (30–1800, default 300), both `.optional().default(...)` so an omitted value still produces a bounded run rather than `deadline` staying `null` forever. `use-chat.ts`'s two `runSpawnAgent(...)` call sites now pass both through.
  - **Bug found via live manual testing, now fixed: scheduled wakeups never actually sent.** User tested the basic scenario (backgrounded shell command + 30s `scheduleWakeup`) in a real terminal — the wakeup fired and visibly queued, but then sat there forever; not even sending a new message drained it. Root cause: the pre-existing auto-drain effect only re-runs on `chat.status` *transitions* (deps `[chat.status]`) — written for messages queued mid-turn, drained the moment status flips to `ready`. But a wakeup timer fires long after its scheduling turn ended, with `chat.status` already sitting at `ready` continuously — pushing onto `messageQueue` doesn't itself change `chat.status`, so the effect never re-fires and the message is stuck. This is the flip side of the closure-staleness reasoning from earlier: routing everything through the queue avoided one bug (reading a stale `chat.status`/calling a stale `chat.sendMessage`) but introduced a worse one (silent non-delivery). Fixed with a new `chatStatusRef` (mirrors `chat.status` every render, same pattern as the pre-existing `messageQueueRef`) — `queueMessage` now checks it live at firing time: genuinely busy (`submitted`/`streaming`) → queue as before (that case ends in a real transition, so the existing drain effect still works); otherwise (the common case, since the parent has usually gone idle by firing time) → call `chat.sendMessage(...)` directly. Safe because `chat.sendMessage` is a stable method (fine to call from an old closure, same as this file's existing `chat.addToolOutput` calls), unlike `chat.status`, a plain snapshotted value — the fix reads the *live* value via the ref instead.
  - Verified via `bun run typecheck` (clean across all four packages) and, for the core schedule → wait → auto-continue round trip, a live manual test in the user's own terminal after the `chatStatusRef` fix — confirmed working (session auto-continued and checked the backgrounded PID on its own with no further input). **Still not verified live**: a background-spawn → check round trip, the task-settle-wins-first race, the unmount-cancels-background-work path, the default-delivery path, and a genuine timeout/max-steps scenario.
  - **Follow-up, found via that same live test: `ShellView`'s success/failure indicator was misleading for checks like `ps -p <pid>`.** The model ran `ps -p <pid>` to check whether the backgrounded `sleep` had finished — a non-zero exit code there is the expected, informative result ("no such process"), not a failure — but `ShellView` (`packages/cli/src/components/tool-view/shell.tsx`) flagged any non-zero `exitCode` as failed, showing a red `✗ 1` even though the check succeeded exactly as intended (confirmed by the model's own correct follow-up text). Fixed by separating the two concepts that were conflated: whether the *tool itself* ran successfully (already tracked separately via the `error` prop, only set when `runShellCommand` actually throws — e.g. a genuine spawn failure — and rendered as a distinct red text block, untouched by this fix) versus the *command's own* exit code, which is just data. Removed the `failed` variable entirely; the header indicator now always shows a green `✓` once the command completes (regardless of exit code), with the exit code appended neutrally when non-zero (`✓ (exit 1)`) instead of driving a red/green split.
  - **Second follow-up, same review**: that fix left genuine tool-execution failures (the `error` prop) with no header indicator at all — `done` requires `result !== null`, and `result` is `null` when `runShellCommand` actually throws, so none of the header's branches matched and it silently rendered nothing there (the full error message still showed at the bottom, just no quick-glance badge up top). Added an `error ? <text fg={colors.error}>✗</text>` branch ahead of the `done` check, so a real execution failure gets its own red `✗` in the header again, distinct from the exit-code-based green indicator.
  - **Third follow-up, found from a live background sub-agent test hitting `maxTurns: 2`**: the test surfaced an inconsistency between `bot-message.tsx`'s task-id displays — `spawnAgent`'s view showed the full task id (`da23eaa4-2cd`, all 12 characters), while `checkAgentTask`'s view and `scheduleWakeup`'s `waitingOnTaskId` mention both truncated it to 8 characters (`taskId.slice(0, 8)`), dropping the `-2cd` suffix — making it harder to visually match a "Checked task" line back to the "Subagent" line it referred to. Since task ids are already a short 12 characters, truncation had no real benefit; removed `.slice(0, 8)` from both spots so all three views show the same full id.
  - **Fourth follow-up, same test**: the max-steps fallback itself showed the bare placeholder ("reached maximum steps without producing any output") with nothing else, even though the sub-agent had made real tool calls — `collectPartialProgress` only ever collected `type: "text"` parts, and with just 2 turns the sub-agent had room to call tools but never narrate or conclude, so there was no text to collect at all. Added `summarizeToolCall(part)` (`spawn-agent.ts`) — a compact `toolName(args)` one-liner, same args-joining approach `bot-message.tsx`'s generic fallback view already uses — and `collectPartialProgress` now includes a `→ toolName(args)` line for every tool call alongside any text, so a run that hit its limit mid-tool-call-chain shows what it was doing instead of a bare "no output" message.
  - **Fifth follow-up, found from a live `shell run_in_background` test**: `ShellView`'s "pid NNNN" spinner never stopped, even long after the process had actually exited (confirmed the `sleep 60` had finished and the touched file existed). Root cause: for background mode, `runShellCommand` returns `{ pid, message }` once, at spawn time, with no `exitCode` — the tool call's output never updates again, so `isBackground` (gated on `exitCode === undefined`) stayed true forever regardless of real process state; nothing tracked whether the PID was still alive. New `packages/cli/src/lib/background-process-status.ts` (`registerBackgroundProcess`/`markProcessExited`/`getProcessStatus`/`onProcessExited` — same one-shot-subscribe shape as `agent-background-tasks.ts`'s `onAgentTaskSettled`, including the already-settled-fires-on-microtask trick) tracks PID liveness. `runShellCommand` (`shell.ts`) now calls `registerBackgroundProcess(proc.pid)` and subscribes `proc.exited.then((exitCode) => markProcessExited(proc.pid, exitCode))` for backgrounded spawns. `ShellView` (`shell.tsx`) gained `useState`/`useEffect` (hooks placed *before* the component's existing input-validity early returns, to satisfy the rules of hooks) subscribing via `onProcessExited` for the shown PID, switching the header from the "pid NNNN" spinner to a green `✓ (background, exit N)` once it actually exits. Same process-lifetime-only caveat as the rest of this session's registries: a PID from before a CLI restart won't be tracked.
  - **Correction, caught by the linter**: the effect initially also called `setExitedStatus(getProcessStatus(...))` synchronously in its body — flagged as exactly the "calling setState directly within an effect" anti-pattern React warns against. It was redundant anyway: the `useState` lazy initializer already captures the correct value at mount, and `onProcessExited` already fires its callback via `queueMicrotask` (not synchronously) when the process already exited by subscribe time — so `setExitedStatus` only ever needed to run from within that subscription callback, never inline in the effect body. Removed the redundant line.
  - Specs: `context/feature-specs/40-wakeup-tool.md`, `context/feature-specs/13-subagent-tool-implementation.md`.

- **Background task result delivery for backgrounded shell commands + assistant-side rendering for all background deliveries (Feature 41)** — Two gaps found while investigating why a backgrounded shell command's spinner never stopped after a CLI restart: (1) `run_in_background: true` shell commands had no way to ever tell the model their result, not even within the same live session — `spawnAgent`'s `runInBackground` had a full completion pipeline (registry, default delivery, `scheduleWakeup` pairing); `shell` never got the equivalent. (2) Background deliveries (already shipped for `spawnAgent`) render via `chat.sendMessage()` as an ordinary `role: "user"` bubble, indistinguishable from something the human typed.
  - **Registry generalized**, not paralleled: `agent-background-tasks.ts` renamed back to `background-tasks.ts`, types/functions genericized (`AgentBackgroundTask` → `BackgroundTask`, `createAgentBackgroundTask`/`completeAgentBackgroundTask`/`failAgentBackgroundTask`/`getAgentBackgroundTask`/`onAgentTaskSettled` → `createBackgroundTask`/`completeBackgroundTask`/`failBackgroundTask`/`getBackgroundTask`/`onTaskSettled`) — reverses the earlier `Agent`-prefixed rename now that its stated reason ("used for only agent background task") no longer holds. `createBackgroundTask(name, description, id?)` — `id` defaults to a generated uuid slice (spawnAgent, no natural id) or is passed explicitly as the stringified PID (shell). This lets `scheduleWakeup`'s `waitingOnTaskId` and default-delivery work identically for both kinds of task without ever needing to know which kind a given id refers to.
  - **Shell wiring** (`shell.ts`): `run_in_background` switched `stdout`/`stderr` from `"ignore"` to `"pipe"` (needed to capture output), registers a task at spawn, and on `proc.exited` settles it with a formatted delivery message (`Background shell command (PID <pid>, "<command>") finished — exit <code>.\n\nOutput:\n<output>`), tail-truncated via a new `truncateTail` helper (`tools/utils.ts` — keeps the tail and drops from the top, the opposite of the existing head-truncating `truncate`, since the most recent output is what the agent needs to act on). A non-zero exit code still calls `completeBackgroundTask` (not `failBackgroundTask`) — same reasoning as the earlier `ShellView` fix, a non-zero exit can be a normal informative result; `failBackgroundTask` is reserved for the delivery pipeline itself throwing.
  - **Default delivery for shell** added in `use-chat.ts`'s *generic* dispatch path (not a new `onToolCall` special case like `spawnAgent`/`scheduleWakeup` — those intentionally bypass the permission gate and hooks, which regular `shell` calls must still go through). After `executeLocalTool` resolves for `toolName === "shell"` with a background-shaped output (`{ pid, message }`, no `exitCode`), a default `onTaskSettled` listener is registered keyed by `String(pid)`, reusing the same `_defaultTaskListeners` map `spawnAgent` already populates (no id collision risk — agent ids are uuid-slice hex+dashes, shell ids are pure-digit PIDs).
  - **Assistant-side rendering**: `ChatMessageMetadata.origin?: "background-task"` added (`packages/shared/src/chat.ts`). `queueMessage` (`use-chat.ts`) takes an optional `origin` param, threaded through `QueuedMessage.origin` and the auto-drain effect so a queued (not just immediately-sent) delivery still carries the tag. All delivery call sites now pass `"background-task"`: the `spawnAgent` default listener, both `scheduleWakeup` fire paths (funnel through the shared `fire()` helper, so one change covered both), and the new shell default listener. `ChatMessage()` (`session.tsx`) checks `msg.role === "user" && msg.metadata?.origin === "background-task"` first and renders via `<BotMessage parts={[{ type: "text", text }]} .../>` instead of `<UserMessage>` — the wire-level role stays `"user"` (required for the model to react to it as a turn); only the local render choice changes. Checked other `role === "user"` call sites (`countMessagesBeforeLastBoundary`, the transport's prior-message continuation check, escape-to-delete-last-turn, the server's image-placeholder scan) — all key off wire-level role or a separate `type` field, unaffected.
  - Tool descriptions (`schemas.ts`, both `shell`'s top-level and `run_in_background`'s field description) and system prompt rule 7 (`system-prompt.ts`) updated to state delivery is automatic for backgrounded shell commands too, and that `waitingOnTaskId` accepts either a `spawnAgent` `taskId` or a shell PID (as a string).
  - **Correction, code review**: `shell`'s top-level description initially example'd `run_in_background` for "a long-running server or build" — wrong, `serverStart` already owns the dev-server case (spawns, then polls the port). Reworded to "build/test/watch command" with an explicit pointer to `serverStart`.
  - **Bug found via code review: `timeout` was silently ignored for `run_in_background`.** Only ever read in the foreground path; passing `timeout` alongside `run_in_background: true` was a silent no-op. Fixed: foreground still defaults to 30s (`timeout ?? DEFAULT_TIMEOUT`); background gets no default (backgrounded commands are expected to potentially run indefinitely by design) but honors an explicit `timeout` by killing the process and delivering a distinct `"timed out after <ms>ms and was killed"` message instead of a normal exit message.
  - **Bug found via live testing: `ShellView`'s background status showed "unknown" the entire time a process was running, jumping straight from that to success** — no intermediate "pid NNNN" spinner ever appeared. Root cause: `ShellView` first mounts while the tool call is still `pending`, before `output`/the real PID exist — so `exitedStatus`'s `useState` lazy initializer (which only ever runs once, at that first mount) permanently froze on `"unknown"`, and the later `[backgroundPid]`-keyed effect only subscribed for *future* exits, never re-syncing the *current*, already-knowable status once the real PID showed up. Fixed by defaulting state to `{ exited: false }` and moving all status determination into the effect. This tripped `react-hooks/set-state-in-effect` on the direct fix (synchronous `setState` from an `if` branch inside the effect body) — resolved by pushing the "untracked PID → predates this process" determination into `onProcessExited` itself (`background-process-status.ts`), which now always fires its callback asynchronously for all three cases (untracked, already-exited, or subscribed-and-later-exits), leaving `ShellView`'s effect a pure subscribe with no direct `setState` of its own.
  - **Bug found via live testing: backgrounded shell processes and in-flight background sub-agents survived `/exit` and Ctrl+C, left running as orphans.** Confirmed via `ps` — a backgrounded `sleep 60` was still running after `/exit`, but correctly killed after `/new` (an in-app session switch that does unmount `Session` and run its cleanup effect). Root cause: both `/exit`'s handler (`input-bar.tsx`) and the SIGINT/SIGTERM `shutdown()` handler (`index.tsx`) call `process.exit()` directly, never unmounting the React tree — so the per-session cleanup effect that calls `cancelAllBackgroundWork(sessionId)` never runs, and a plain `process.exit()` doesn't itself signal `Bun.spawn` children or abort in-flight fetches. Fixed with a new `cancelAllRegisteredWork()` (`session-background-work.ts`, walks every session's registered work, not just one) called explicitly right before `process.exit()` in both places.
  - **Follow-up, from live feedback: plain-text delivery rendering looked like the assistant repeating itself, not a structured result.** Added `ChatMessageMetadata.backgroundTaskView?: { label, taskId, status, output }` set directly at the point of delivery (not parsed back out of the text); new `BackgroundTaskMessage` component renders it as a labeled result card (`Background Task › label (id ...)` header + ✓/✗ + `OUT` lines in a bordered box), mirroring `ShellView`'s IN/OUT look instead of plain assistant prose. Scoped to `spawnAgent`'s and shell's default-listener deliveries only — `scheduleWakeup`'s two fire paths keep the plain-text fallback, since their fired text mixes the model's own free-form `prompt` with an optionally-appended task outcome, not a clean single label/output pair.
  - **Follow-up, from live feedback: a background-task delivery queued while busy showed up in the visible "N queued" panel**, looking like a real message the user was waiting to send. Fixed at the display layer only — `session.tsx` now passes a `visibleMessageQueue` (filters out `origin === "background-task"`) as the queue panel's `queue` prop, while the auto-drain effect keeps reading the full unfiltered queue so drain order/timing is unchanged. This broke index-based queue removal (a focused position in the filtered display no longer matched the same position in the full array), fixed by giving `QueuedMessage` a stable `id` and changing `removeFromQueue` from index-based to id-based.
  - Not yet re-verified live after the fixes above; the underlying delivery pipeline itself (shell auto-delivery, `scheduleWakeup` against a shell PID, the assistant-side card render, hidden-from-queue-panel behavior) is still only verified via `bun run typecheck`/lint and code review.
  - Spec: `context/feature-specs/41-background-task-result-delivery.md`.

- **`serverStart` log visibility + explicit/implicit stop (Feature 24 follow-up)** — Found while working on Feature 41: `runServerStart` spawned with `stdout`/`stderr: "ignore"`, so a failed startup returned only `"Timed out waiting for port N"` with no information about why (missing dependency, syntax error, port already in use); separately, nothing ever registered a started server for cancel-on-unmount, so it could survive `/exit`/Ctrl+C the same way backgrounded shell commands did before that fix, with no explicit stop tool either.
  - Switched `stdout`/`stderr` to `"pipe"`; new PID-keyed, tail-capped log buffer (`lib/background/server-log-buffer.ts`) is fed by two background reader loops that start at spawn and run for the process's entire lifetime — not just during the readiness poll — so a timeout now returns the actual captured output (e.g. a stack trace) directly in its error, and logs stay available for later inspection while the server keeps running.
  - New `checkServerLogs({ pid })` tool — deliberately a pull, not a push/auto-delivery like backgrounded shell's, since a dev server has no "finished" event to hang a one-shot delivery off. New `serverStop({ pid })` tool (mirrors `browserClose`) sends `SIGTERM` via the global `process.kill` (only has the PID, not the original `Bun.Subprocess` handle, since it's a separate call from `serverStart`'s). `runServerStart` also now registers with `session-background-work.ts` (same `registerBackgroundWork` shell already uses) for implicit cleanup on unmount/`/exit`/Ctrl+C. Both new tools gated the same as `serverStart` itself (`browserToolContracts`, BUILD + opt-in browser flag).
  - System prompt's browser-control workflow section updated to mention both new tools.
  - Not yet verified live. Spec: `context/feature-specs/24-browser-control-integration.md` (Follow-up section).

- **Prompt caching (Anthropic direct + via OpenRouter)** — Cost-optimization feature: since chat APIs are stateless, every turn re-sends the full system prompt, tools, and history, so cost compounds as a session grows. Implemented explicit `cache_control` breakpoints for the direct-Anthropic-key path, plus a separate, simpler mechanism for Anthropic models routed through OpenRouter (Google's context-caching lifecycle and custom/Ollama providers remain out of scope — see spec for why). Went through two design corrections on the direct-Anthropic path before landing on the right shape — worth reading in full in the spec's Status section, since the wrong version would have looked like it worked (typechecks fine) while silently never hitting cache on the one breakpoint that mattered:
  1. First tried reordering the system prompt's volatile `ideActiveFile` section (changes turn-to-turn as the user switches editor tabs) to the end of the joined string — insufficient, since Anthropic's `cache_control` invalidates a whole content block on any change regardless of position within it.
  2. Then split system into two separate `SystemModelMessage`s (`stable` cached, `volatile` uncached) — still wrong, because Anthropic's caching is a *cumulative prefix* across the fixed canonical order `tools → system → messages` (confirmed against Anthropic's own docs, not assumed): a breakpoint's hit depends on everything before it in that order, so `ideActiveFile` sitting anywhere in `system` — split out or not — still sat upstream of the `messages`/history breakpoint and silently spoiled it (the `tools` breakpoint, canonically first, was never actually at risk). Caught by the user asking "doesn't the volatile spoil the whole thing?" before this shipped anywhere live.
  3. **Final fix:** `ideActiveFile` removed from the system prompt entirely — `buildSystemPrompt` (`packages/server/src/prompts/system-prompt.ts`) is back to one fully-stable string, no split. New `appendIdeContext` (`packages/server/src/lib/prompt-caching.ts`) appends the IDE-context text directly onto the *newest message's* own content instead — that message is already a fresh, uncached write every turn, so attaching volatile text there is free, and once finalized it never threatens future turns' cache hits. Skipped when the last message is a tool-result continuation (`role: "tool"`, no text-part slot in `ToolContent`) — accepted trade-off, not a bug.

  Remaining pieces: `packages/server/src/lib/prompt-caching.ts` also has `buildCachedSystemMessage` (single cached system message now, no volatile counterpart), `withToolsCacheControl` (marks the last tool definition, closing `tools` as one cached prefix), and `withHistoryCacheControl` (marks the newest message every turn — now actually reliable, since nothing volatile sits upstream of it anymore; `appendIdeContext` runs first so the breakpoint's hash covers the final content). `ResolvedModel` (`packages/server/src/lib/models.ts`) gained `promptCaching?: boolean`, set `true` only in `resolveAnthropicModel`'s direct-key branch — deliberately not inferred from `provider === "anthropic"` alone, since that label is also returned on the OpenRouter-fallback path, which uses an entirely separate mechanism (below), not `chat.ts`'s manual breakpoint helpers. `packages/cli/src/lib/cost.ts`'s `estimateSessionCost` updated to price `usage.inputTokenDetails.{cacheReadTokens,cacheWriteTokens,noCacheTokens}` at their real rates (read ≈0.1× input rate, write ≈1.25×) instead of billing all input tokens at the flat rate — falls back to the exact prior flat-rate formula when a provider doesn't report the breakdown. `/agent-step` (single-step, ephemeral, no accumulating history) intentionally left untouched — out of scope for this pass.

  **Correction, same session:** OpenRouter pass-through was initially written off in the spec as "unresolved, don't commit to it" — wrong, the user pushed back ("openrouter implements prompt caching for providers that support it") and it checked out against both OpenRouter's own docs and the installed `@openrouter/ai-sdk-provider@2.9.0` type declarations (`OpenRouterChatSettings.cache_control: { type: "ephemeral", ttl?: "5m"|"1h" }`, confirmed in `node_modules`). OpenAI-via-OpenRouter caches automatically with no config, same as calling OpenAI directly. Anthropic-via-OpenRouter gets a top-level `cache_control` setting passed once at model-construction time — OpenRouter auto-advances the breakpoint itself as the conversation grows, so none of `chat.ts`'s manual per-turn breakpoint bookkeeping is needed for this path. Implemented entirely inside `resolveViaOpenRouter` (`packages/server/src/lib/models.ts`): `openrouter.chat(routerModelId, settings)` now passes `{ cache_control: { type: "ephemeral" } }` when `provider === "anthropic"` — exact gate, not a heuristic, since the app's `"openrouter"`-native model list has no `anthropic/*` entries today. Zero changes to `chat.ts` needed for this path.

  Spec: `context/feature-specs/36-prompt-caching-support.md`. **Not verified in-agent:** OpenAI's automatic caching actually landing, and Anthropic's cache hit rate in a live session (direct or via OpenRouter) — all need a real provider account to confirm; `bun run typecheck` is clean but no live request was made.

- **Info sidebar display** — Optional right-hand panel in the Session screen showing session title, context usage (tokens/%/cost), MCP server list, and modified files — off by default, toggled via `--info` CLI flag or `/info` command. `infoSidebarVisible` state added to `PromptConfigProvider`, persisted to `~/.koincode/config.json` (new `infoSidebarVisible` field on `KoincodeGlobalConfig`) the same way `voiceInput` already is — initial state is `--info` OR the persisted value, and `toggleInfoSidebar` writes back on every toggle, so the sidebar stays in whatever state the user last left it in across app restarts (no need to keep passing `--info` or re-toggling every launch). `toggleInfoSidebar` threaded through `CommandContext` → `input-bar.tsx` → `/info` command entry in `commands.tsx`. New `InfoSidebar` component (`components/info-sidebar.tsx`) renders alongside the existing chat column in `session-shell.tsx` (wrapped in a `flexDirection="row"` box; the chat column dropped its `width="100%"` in favor of `flexGrow`/`flexShrink` so the sidebar gets real space instead of being pushed off-screen), background matches `colors.surface` (same as the input bar / message boxes). The Modified Files section is collapsible exactly like the "thinking" block in `bot-message.tsx` (click-to-toggle header with a ▾/▸ chevron via `onMouseDown`, expanded by default) and shows *all* files (no cap) in a `flexGrow`-ing `<scrollbox>` so a long list scrolls internally instead of stretching the sidebar past the terminal height. Three net-new data sources: (1) **cost** — `lib/cost.ts`'s `estimateSessionCost` sums per-assistant-turn cost using each message's `metadata.usage` against the model's pricing (`findSupportedChatModel` for built-ins, `listCustomModels()` for `custom/*` ids), exposed from `useChat` as `sessionCost`; (2) **modified files** — `lib/git-status.ts`'s `getModifiedFiles()` shells out to `git status --porcelain` + `git diff --numstat HEAD` (same `execSync` pattern as `getGitBranch()`), read via a 3s-polling `useModifiedFiles(enabled)` hook that only runs while the sidebar is visible; shows the repo's full current dirty state, not scoped to this session (simpler, no tracking state needed, and surfaces pre-existing uncommitted changes too) — untracked files get an all-additions line count read directly off disk since they don't appear in `diff --numstat`; (3) **MCP server list** — `useMcpServerCount` in `status-bar.tsx` was replaced with a shared `hooks/use-mcp-servers.ts` (`useMcpServers()`) returning the full per-server array, so the sidebar and status bar share one fetch. LSP status (present in the OpenCode reference image that inspired this feature) was scoped out — KOINCODE has no language-server integration. Spec: `context/feature-specs/35-info-sidebar-display.md`.
  - **Bug found and fixed along the way:** `contextUsage` (tokens/%/cost shown in both the `/context` dialog and this sidebar) computed its context-window denominator from the *last assistant message's* model rather than the currently selected one — so switching models updated the displayed name immediately but left the percentage/window size stale until the next request went out. Fixed in `use-chat.ts`: the window now resolves against the live selected model (`usePromptConfig().model`), only reusing the last message's server-reported `contextWindow` (relevant for Ollama/custom models) when that message's model still matches what's currently selected.

- **Standalone binary distribution** — Replaced multi-step install flow with a single self-contained binary per platform. Built with `bun build --compile`, distributed via GitHub Releases, installable with a one-line curl command.
  - **Build script:** `packages/cli/bin/build.sh` gains a `COMPILE=true` mode that produces `koincode-darwin-arm64`, `koincode-darwin-x64`, and `koincode-linux-x64` via `bun build --compile --target`. Playwright kept external in compiled binaries (gated at runtime).
  - **Server bundling:** CLI entry point (`packages/cli/bin/koincode.ts`) checks for `--server` flag — when present, imports `@koincode/server` and skips the CLI entirely. This lets the compiled binary spawn itself as a background server.
  - **Server manager:** `packages/cli/src/lib/server-manager.ts` detects three runtime modes: dev (source TS), npm (bundled `server.js`), compiled binary (no `server.js` → spawns `process.execPath --server`).
  - **Playwright gating:** Browser tools are now opt-in. `browserTools: boolean` added to `KoincodeGlobalConfig`. Enable via `koincode --enable-browser-tools` (CLI flag) or `/enable-browser-tools` (command menu). `browserToolContracts` exported separately from `@koincode/shared/schemas`; `getToolContracts(mode, browserTools)` conditionally merges them. Chat route accepts `browserTools` in the request body, passed from `use-chat.ts` transport. System prompt only includes browser control section when enabled. `browser-session.ts` uses dynamic `import("playwright")` with browser resolution logic (`packages/cli/src/lib/browser-setup.ts`): checks system Chrome first, then `~/.cache/ms-playwright/`, caches the result in config.
  - **CI:** `.github/workflows/release.yml` — triggered on `v*.*.*` tag push, builds macOS (arm64+x64) and Linux (x64) binaries on platform-appropriate runners, uploads as GitHub Release assets with auto-generated notes.
  - **Install script:** `install.sh` at repo root — detects OS/arch, downloads latest release binary, installs to `/usr/local/bin` (or `~/.local/bin`), clears macOS quarantine flag.
  - **README:** Updated with one-line install command, npm alternative in collapsible details, browser tools opt-in docs, and standalone binary build instructions.
  - Spec: `context/feature-specs/xx1-standalone-binary-distribution.md`.

## Recently Completed (Phase 2 — continued)

- **IDE file awareness** — When KOINCODE runs inside a VSCode integrated terminal, the status bar now shows `In <filename>` (dim, right-aligned) for whatever file is active in the editor. Two-part implementation: (1) `packages/vscode-extension/` — a minimal VSCode extension (`koincode-vscode`) that activates on startup, writes `~/.koincode/ide-context.json` with the active editor path on every `onDidChangeActiveTextEditor` event, and clears it on deactivate; (2) `packages/cli/src/hooks/use-ide-context.ts` — a React hook that only activates when `TERM_PROGRAM === "vscode"`, reads the context file on mount, and watches `~/.koincode/` via `fs.watch` for subsequent changes. `StatusBar` gained an `activeFile` prop rendered between the left indicators and the context ring. `IDE_CONTEXT_FILE` constant added to `@koincode/shared/paths`. Spec: `context/feature-specs/29-ide-file-awareness.md`.

## Recently Completed (Phase 2 — continued)

- **`/update` slash command** — Users can now run `/update` from any session to update koincode in-place. `packages/cli/src/lib/update.ts` exposes `checkForUpdate()` (fetches npm registry, returns new version or null) and `runUpdate(destroyRenderer, version)` (tears down the TUI, SIGTERMs the background server via `PID_FILE`, then spawns `npm install -g koincode` with `stdio: "inherit"` so the user sees output and can enter their sudo password). The command action shows toasts for each step and handles three outcomes: no update available, update found → install, and network/registry error. `CommandContext` gained a `destroyRenderer` field wired from `useRenderer()` in `input-bar.tsx`. `useUpdateCheck` was refactored to broadcast the result to all mounted consumers via a module-level subscriber set, so the "update available · /update" badge now appears in both the home screen and the session status bar. Spec: `context/feature-specs/27-in-app-update-command.md`.

- **Browser control integration** — Autonomous browser testing loop implemented. Background shell execution: `shell` tool gains `run_in_background` flag (spawns detached, returns PID immediately). `serverStart` tool starts a server in background and polls the port with `Bun.connect` until ready — avoids the `>/dev/null` write-redirection false positive in the permission classifier. Seven browser tools in `packages/cli/src/tools/browser/`: `browserNavigate`, `browserScreenshot`, `browserClick`, `browserType`, `browserGetConsoleLogs`, `browserClose`, plus `serverStart`. Playwright session held in module-level state (lazy init on first navigate, torn down on `browserClose`). Screenshots compressed to JPEG 85% and returned as multimodal content (`[{type:"image",...},{type:"text",...}]`) for vision models; text-only fallback for non-vision models via `isVisionModel()`. `vision: boolean` added to all models in the registry. `executeLocalTool` gains optional `modelId` param threaded from `use-chat.ts` and `spawn-agent.ts`. `serverStart` gets its own permission entry (`shell:bin:serverStart`, label "Start server"). Browser control section added to BUILD mode system prompt. Spec: `context/feature-specs/24-browser-control-integration.md`.

- **MCP support (Phase 1 + 2)** — Full implementation complete. Phase 1: server connects to MCP servers at startup via `mcp-manager.ts`, merges their tools (namespaced `serverName__toolName`) into every chat request. Phase 2: per-session approval gate before first MCP tool call; `manageMcp` read-only tool for the agent to inspect server status; `GET /mcp/servers` + `POST /mcp/call` routes; MCP server count shown in the status bar (`› N mcp`). See `context/feature-specs/23-mcp-support-integration.md`.


- **Context management** — `/context` command opens a modal showing tokens used vs. model context window (single bar, exact from API usage data). `/compact` command summarizes the conversation via the AI, stores a `compact_boundary` marker server-side, and resets the LLM context to just the summary + new messages — old messages remain visible in the transcript with a "Context compacted" divider. Status bar shows a 10-segment dot ring (`●●●●●●●●○○`) when usage ≥ 80%, turning red at 95%. Auto-compaction triggers automatically at 90% between responses. `getContextWindow(modelId)` added to `@koincode/shared` with per-model sizes for all supported models. `contextUsage` (tokensUsed, contextWindow, percent) exposed from `useChat` and threaded through SessionShell → InputBar → StatusBar. New server route `POST /sessions/:id/compact`; chat boundary scan now recognizes both `clear_boundary` and `compact_boundary`. Feature spec: `context/feature-specs/21-context-management.md`.

- **Voice input** (infrastructure complete, UI disabled) — `/voice` command and `ctrl+r` toggle are commented out pending a reliable recorder bootstrap. Infrastructure is in place: `packages/cli/src/lib/whisper.ts` handles cloud transcription (OpenAI `whisper-1` or OpenRouter `openai/whisper-large-v3`, auto-selects based on which key is present); `packages/cli/src/lib/voice-recorder.ts` handles recording (macOS: compiled Swift/AVFoundation binary at `~/.koincode/bin/koincode-recorder`; Linux: `arecord`; Windows: PowerShell MCI). Config fields: `voiceInput`, `whisperBackend` (`auto`/`openai`/`openrouter`). Blocker: compiling the Swift binary at runtime via `swiftc` is fragile (requires Xcode CLT) — see Deferred for the fix.

- **Skills system** — Command menu extended with skills: reusable task instructions the agent reads and executes. Each skill is a directory (`SKILL.md` + optional `scripts/`, `references/`, `assets/`) stored at `.koincode/skills/` (project-local) or `~/.koincode/skills/` (global). Two built-in skills shipped: `code-review` and `git-commit`. The skills manifest is injected into the system prompt on every request so the agent knows what's available. Two new tools: `readSkill` (PLAN+BUILD — reads SKILL.md and directory listing, or a specific sub-file with path-traversal guard) and `writeSkill` (BUILD-only — creates or updates SKILL.md, detects create vs. update, preserves existing sub-directories). Skills appear in the command menu alongside built-in commands; selecting one triggers an immediate agent turn. Works from both the home screen (creates a new session) and inside an existing session. Agent can also create/update skills on request via `writeSkill`. Files: `packages/cli/src/lib/skills.ts`, `packages/cli/src/skills/builtins.ts`, `packages/cli/src/tools/read-skill.ts`, `packages/cli/src/tools/write-skill.ts`.

## Recently Completed

- **Data visualization via browser** — Added visualization guidance to the BUILD mode system prompt. When a user asks to chart or visualize data, the agent creates a self-contained HTML file using Chart.js or Plotly (CDN-linked), embeds data inline, saves it, and opens it in the default browser. No new tools or schema changes — leverages existing `writeFile` and `shell` tools. Spec: `context/feature-specs/31-visualization-via-browser.md`.

- **Local model support** — Users can now chat with locally-running models (Ollama, LM Studio, vllm, or any OpenAI-compatible endpoint). `GET /local-models` on the server auto-detects Ollama at `localhost:11434` and returns pulled models alongside user-configured custom endpoints. The model picker gains a third "Local" tab (Tab to cycle) that fetches and lists discovered models with file size hints. Model IDs use an `ollama/<name>` or `local/<name>` prefix convention. `resolveChatModel` in the server handles both by wiring `createOpenAI` with the appropriate `baseURL`. `ollamaBaseURL` (and `localModels`) added to `KoincodeGlobalConfig` for users with non-default Ollama URLs. The model type throughout the CLI was broadened from `SupportedChatModelId` to `string` to accommodate arbitrary local model IDs. **Superseded by Extra model / provider support (Phase 1) below** — the `local/` prefix, `LocalModelConfig`, and single "Local" tab no longer exist.

- **Tool refactor** — `packages/cli/src/lib/local-tools.ts` split into `packages/cli/src/tools/` with one file per tool (`read-file.ts`, `list-directory.ts`, `glob.ts`, `grep.ts`, `write-file.ts`, `edit-file.ts`, `bash.ts`). Shared helpers/constants live in `tools/utils.ts`. `tools/index.ts` exports `executeLocalTool` with the same switch-based dispatch. Import in `hooks/use-chat.ts` updated; old `lib/local-tools.ts` deleted.
- **Tool call styling** — `editFile` now renders a diff view: removed lines in red with `- ` prefix, added lines in green with `+` prefix, capped at 8 lines per side with truncation notices. `writeFile` shows a file creation preview: header with path + first 3 lines of content (dimmed), with `…` if the file has more. Both components show a ` …` spinner while the tool is pending and surface error text if the call fails. All other tools retain the existing plain-text display. Change is in `packages/cli/src/components/messages/bot-message.tsx`.
- **Permission / approval system** — When the agent calls `shell`, `writeFile`, or `editFile`, the CLI intercepts the tool call before execution and replaces the input bar with an approval widget. The user picks Allow once, Allow for project (persisted to `.koincode/config.json`), or Deny. Deny sends `{ denied: true }` back to the model. Routine file writes inside the project are auto-allowed; sensitive paths (`.env`, `*.pem`, `.git/config`, `.github/workflows/**`, etc.) always require approval. Shell commands are grouped by binary into typed permission keys (`shell:git`, `shell:npm`, `shell:rm`, `shell:write`, `shell:unknown`) with destructive-tier visual indicators. Concurrent tool-call approvals are serialized via a shared Promise mutex. Implemented in `packages/cli/src/lib/permissions.ts`, `packages/cli/src/lib/project-config.ts`, and `packages/cli/src/components/approval-widget.tsx`.
- **`askUser` tool** — Model-initiated question tool available in both PLAN and BUILD modes. The model calls `askUser({ question, options, allowFreeText })` mid-task to ask the user a multi-option question. The same input-bar slot renders `AskUserWidget` with arrow key / number shortcut navigation and an optional free-text input mode. The user's selected value (or `{ cancelled: true }`) is returned as the tool result. Uses the same shared mutex as the permission system so approvals and questions are never shown simultaneously. Schema in `packages/shared/src/schemas.ts`; widget in `packages/cli/src/components/ask-user-widget.tsx`.
- **Typecheck script** — `bun run typecheck` added to root `package.json`. Runs `tsc --noEmit` across `packages/shared`, `packages/cli`, and `packages/server` in sequence.

- **Extra model / provider support (Phase 1)** — Consolidated the `local/`-only custom model path into a provider/model split so hosted OpenAI-compatible providers (Groq, Mistral, Cerebras, etc.) and self-hosted servers share one mechanism. `packages/shared/src/config.ts`: `LocalModelConfig` replaced by `CustomProviderConfig` (`id`, `name`, `baseURL`, `apiKey?`) and `CustomModelConfig` (`id`, `providerId`, `modelId`, `contextWindow?`, `vision?`, `pricing?`); `KoincodeGlobalConfig.localModels` → `customProviders` + `customModels`. `packages/shared/src/models.ts`: `SupportedProvider`'s `"local"` → `"custom"`; `isLocalModelId` → `isCustomOrOllamaModelId`; dead `LocalModelEntry` type removed (was unused before the rename too). Server (`packages/server/src/lib/models.ts`): `resolveLocalModel` split into `resolveOllamaModel` and `resolveCustomModel` (looks up the model, then its provider by `providerId`, then resolves via `@ai-sdk/openai-compatible`'s `createOpenAICompatible`) — added `@ai-sdk/openai-compatible@^2.0.41` (matched to the repo's existing `@ai-sdk/provider@3.0.8` line; the current `3.x` releases of `openai-compatible` require `@ai-sdk/provider@4.x` / `LanguageModelV4`, which isn't compatible with `ai@^6.0.154` here). CLI: model dialog's "Local" tab split into **Custom** and **Ollama** (4 tabs total: Frontier, Free, Custom, Ollama), Custom tab shows `modelId` directly (no `displayName`). Not yet done: guided add/delete UI (Phase 2) — custom providers/models still require hand-editing `~/.koincode/config.json`. Spec: `context/feature-specs/34-extra-model-support.md`.

- **Ollama resolution fixes** — Found while reworking `resolveOllamaModel` above: it was calling the `@ai-sdk/openai` provider instance directly (`provider(modelId)`), which — as of the currently pinned `@ai-sdk/openai@3.0.52` — defaults to OpenAI's Responses API (`/responses`) rather than Chat Completions, an endpoint Ollama doesn't implement. Fixed en route to the swap below by calling `.chat(modelId)` explicitly. Then swapped `resolveOllamaModel` off the OpenAI-compat shim entirely onto `ollama-ai-provider-v2` (community-maintained, peer-compatible with this repo's `ai@^6`/`@ai-sdk/provider@3.x` pin — the newer `ai-sdk-ollama` package needs `ai@^7` and isn't). This also fixes a real context-truncation risk: Ollama's runtime context window is controlled by `num_ctx`, which the OpenAI-compat path never set, and `getContextWindow()` in `@koincode/shared` falls back to a flat `128_000` for any model outside the curated list (including all Ollama models) — so the CLI's context bar/auto-compaction could show far more headroom than Ollama actually had. `resolveOllamaModel` now calls Ollama's `POST /api/show` per request (`fetchOllamaContextLength`, 3s timeout, scans `model_info` for a `*.context_length` key since it's namespaced per architecture, e.g. `llama.context_length`) and passes the real value as `providerOptions: { ollama: { options: { num_ctx } } }`. `resolveChatModel` and its 3 call sites (`routes/chat.ts` ×2, `lib/helpers.ts`) are now `async` to support the lookup. **Gap closed:** the displayed context-window number now matches what the server actually used. `ResolvedModel` (`server/lib/models.ts`) gained an optional `contextWindow` field — `resolveOllamaModel` sets it from the same `fetchOllamaContextLength` lookup already used for `num_ctx`, and `resolveCustomModel` sets it from `CustomModelConfig.contextWindow` (the value users optionally set in the add-model wizard, which was being persisted but never read anywhere until now). `ChatMessageMetadata` (`@koincode/shared/chat.ts`) gained a matching `contextWindow?: number`; `routes/chat.ts`'s `messageMetadata` callback includes it (both `start` and `finish` parts) whenever `resolvedModel.contextWindow` is known. `use-chat.ts`'s `contextUsage` now reads `metadata.contextWindow ?? getContextWindow(modelId)` — server-reported value wins when present, static curated-list lookup is still the fallback for everything else.

- **Extra model / provider support (Phase 2)** — Guided add/delete flows, replacing hand-edited config as the only way to manage custom providers/models. New shared files: `packages/shared/src/config-schemas.ts` (`customProviderInputSchema`, `customModelInputSchema`, exported from `index.ts`) and `packages/cli/src/lib/custom-models.ts` (`listCustomProviders`/`listCustomModels`, `addCustomProviderWithModel`, `addCustomModel`, `updateCustomProvider`, `deleteCustomProvider` with cascade, `deleteCustomModel` — all opaque `id`s generated via `crypto.randomUUID().slice(0, 8)` prefixed `provider/`/`custom/`). New `packages/cli/src/components/dialogs/model-field-steps.tsx` holds the reusable step primitives — no multi-field-per-screen form or checkbox control existed anywhere in the CLI before this, and OpenTUI has no checkbox/switch renderable, so each wizard is built as a sequence of single-field screens (`TextStep`, mirroring the existing `EditKeyView` single-textarea pattern) plus a 2-option Yes/No list for booleans (`BooleanStep`, mirroring the `approval-widget.tsx` option-list idiom) rather than one combined multi-field screen. `ProviderFieldsWizard` (name → baseURL → apiKey) and `ModelFieldsWizard` (modelId → contextWindow → vision) are separate step machines that chain via callbacks; escaping the first step of the model wizard resumes the provider wizard at its last field (`apiKey`) with prior input intact, rather than losing it. `/setup` (`setup-dialog.tsx`): the 4 built-in provider rows stay fixed/edit-only; a dynamic `customProviders` section below them gets its own rows (name, masked key, model count) with Enter to edit and Ctrl+D to delete, plus a "+ Add provider" row that runs `ProviderFieldsWizard` then `ModelFieldsWizard` and writes both via `addCustomProviderWithModel` in one call. `/models` Custom tab (`models-dialog.tsx`): switched from fetching custom models via the server's `GET /local-models` round-trip to reading `customModels`/`customProviders` directly from local config (matching how `/setup` already works — no reason to round-trip through the server for something the CLI can read off disk itself; only the Ollama tab still needs the server call, since that's live daemon detection). Gains a pinned "+ Add model" sentinel row (`DialogSearchList` has no built-in pinned-row support, so it's prepended to the items array with `filterFn` short-circuited to always keep it) that opens a provider picker then `ModelFieldsWizard`, plus Ctrl+D per-row delete. **Delete UX deviates from the spec's literal wording** ("confirmation showing the count") — instead of a new blocking yes/no modal (no such component exists anywhere in this codebase), both deletes reuse the optimistic-delete-then-5s-undo-window pattern already established in `sessions-dialog.tsx` (`Ctrl+D` deletes immediately + toast with the count, `Ctrl+U` restores, unmount flushes any pending delete) — same recoverability guarantee, but consistent with the one destructive-delete idiom that already exists in this app rather than introducing a second one. Verified via `bun run typecheck` (clean across all 4 packages) and by reading the actual OpenTUI `@opentui/core`/`@opentui/react` type declarations directly (`InputRenderable`/`TextareaRenderable`/`EditBufferRenderable` — confirmed `.setText()`/`.plainText`/`.onSubmit` are inherited and usable on `<input>`, not just `<textarea>`) rather than assuming API shape from the existing dialogs alone. **Not verified in-agent:** could not exercise the new wizard flows interactively — this environment has no real TTY for the OpenTUI renderer, so manual click-through testing had to happen with the user in their own terminal.

**Bug found via that manual testing, now fixed:** the `TextStep` submit wiring initially mirrored `EditKeyView`'s imperative `input.onSubmit = fn` pattern, but that only works for `<textarea>` — pressing Enter on the name field did nothing. Root cause: `InputRenderable.submit()` *overrides* `TextareaRenderable.submit()` to `emit("enter", value)` instead of invoking `._submitListener` (the mechanism `.onSubmit = fn` relies on), so the assignment was silently inert for `<input>`. The declarative `onSubmit` JSX prop looked like the fix, but its type is an unusable intersection of two incompatible library-internal signatures (`(event: SubmitEvent) => void` from the inherited Textarea-core option, `(value: string) => void` from the React-only Input variant) — confirmed by reading `@opentui/react`'s reconciler source (`chunk-dz5xbr9p.js`), which does correctly map that prop to `InputRenderableEvents.ENTER` at runtime, but `tsc` rejects passing a plain string-typed handler to it. Fixed by subscribing directly via `inputRef.current.on(InputRenderableEvents.ENTER, handler)` / `.off(...)` in a `useEffect`, bypassing both the broken imperative property and the broken prop type.

**Second bug found via manual testing, now fixed:** after the Enter-to-advance fix, text typed into one step (e.g. provider name) was carrying over into the next step's input (base URL) — had to be manually deleted before typing the new value. Root cause: `ProviderFieldsWizard`/`ModelFieldsWizard` render one `<TextStep>` per `if (step === ...)` branch, all at the same JSX tree position with the same component type — React doesn't remount between steps, it just re-renders the same `TextStep` instance (and its underlying `<input>` renderable) with new props. The `setText(initialValue)` effect meant to seed each field only re-runs when `initialValue` actually changes by value, and since most fields start `""`, going from one empty field to the next empty field never re-triggered it — so the previous field's typed text stayed in the live buffer. Fixed by giving every `<TextStep>`/`<BooleanStep>` return a distinct `key` (`"name"`, `"baseURL"`, `"apiKey"`, `"modelId"`, `"contextWindow"`, `"vision"`), forcing React to unmount/remount a fresh instance on every step transition.

**Third bug found via manual testing, now fixed:** selecting a custom model showed the opaque internal id (e.g. `custom/1a2b3c`) in the status bar instead of the real model name — correct for routing (`resolveChatModel` needs the opaque id to look up the right provider) but never meant to be user-visible. Added `getModelDisplayName(modelId)` to `packages/cli/src/lib/custom-models.ts` — passes through non-custom ids untouched, resolves `custom/*` ids to their `CustomModelConfig.modelId`. Applied at all 4 render sites that print a bare model-id string to the user: `status-bar.tsx` (current session model), `messages/bot-message.tsx` (per-message historical model, can't reuse the live "current model" value since old messages may reference a model since changed or deleted), and `dialogs/context-dialog.tsx` (×3 render branches). `usage.ts`/`analytics.ts` were checked at the time and didn't need it, since they only used the model id as a lookup/tracking key — see below, this changed shortly after.

**Follow-up cleanup, same session:** three more "local" naming/behavior issues surfaced by inspection of `analytics.ts`, `usage.ts`, and `models-dialog.tsx`:
1. `analytics.ts`'s `resolveProvider` lumped every custom model under the analytics category `"local"`, same as Ollama — added `getCustomProviderName(modelId)` to `custom-models.ts` (model → its provider's `.name`) so custom models now report their real provider (e.g. `"Groq"`) instead of a generic bucket; Ollama still correctly reports `"local"`.
2. `usage.ts`'s `UsageTarget` had the same problem structurally: `{ type: "local" }` covered both Ollama and custom models, so `/usage` told a Groq user "Local model — no usage page to open," which is wrong on two counts (not local, and Groq does have a dashboard we just don't have registered). Split into separate `"ollama"` and `"custom"` variants; `commands.tsx`'s `/usage` handler now shows accurate copy for each ("Local model…" vs "No usage dashboard registered for custom providers").
3. `models-dialog.tsx` was fetching `/local-models` with a raw `fetch()` instead of the typed `apiClient` (Hono RPC client) used everywhere else in the CLI (`apiClient.sessions.$get()`, `apiClient.mcp.servers.$get()`, etc.) — and that endpoint still bundled Ollama detection *and* custom-model config into one response, even though the Custom tab had already been switched to reading config directly (see Phase 2 above) and never consumed the `custom` field. Decoupled fully: `packages/server/src/routes/local-models.ts` → `ollama-models.ts` (Ollama-only now, dropped the now-dead `customModels` config read), route moved from `/local-models` to `/ollama-models`, `LocalModelsResponse` → `OllamaModelsResponse` (shared/models.ts, now just `{ ollama: [...] | null }` — no `custom` field), and `models-dialog.tsx` switched to `apiClient["ollama-models"].$get()`. This also incidentally un-did an accidental circular type import between `models.ts`/`config.ts` in `@koincode/shared`, since `models.ts` no longer needs `CustomModelConfig` from `config.ts` at all.

- **Agent notification support (Feature 37)** — Terminal bell rings when the agent needs the user's attention (turn finished, or an approval/askUser/mode-switch prompt appears) but the terminal window isn't focused. Focus is tracked via DEC private mode 1004 (`\x1b[?1004h`/`l`, enabled/disabled in `index.tsx`); the resulting `\x1b[I`/`\x1b[O` sequences are caught via OpenTUI's `prependInputHandlers` renderer option (`packages/cli/src/lib/terminal-focus.ts`, plain module-level boolean — not React state, since only `use-chat.ts` needs an imperative read). `use-chat.ts` rings the bell (`\x07`) at the streaming/submitted→ready transition (guarded against firing on mount) and at each `setPendingApproval`/`setPendingUserQuestion`/`setPendingModeSwitch` call site. On by default via `notificationEnabled?: boolean` on `KoincodeGlobalConfig`; `/notifications` command toggles it, mirroring the existing `/enable-browser-tools` pattern. Deliberately terminal-bell-only — a title-bar marker was considered too low-visibility and an OS-native desktop notification too disruptive. Spec: `context/feature-specs/37-agent-notification-support.md`.

**Follow-up, found via manual testing in Antigravity (a VS Code fork):** `printf '\a'` produced nothing — VS Code-family integrated terminals ignore BEL by default. Added a fallback that relays through the bundled editor extension instead of failing silently for that whole user population: `packages/cli/src/lib/vscode-notify.ts` (`notifyVsCode`, gated on `TERM_PROGRAM === "vscode"`) writes `{ message, at }` to a new `NOTIFY_REQUEST_FILE` (`~/.koincode/notify-request.json`, `packages/shared/src/paths.ts`); `packages/vscode-extension/src/extension.ts` now also `fs.watch`es for it and relays via `vscode.window.showInformationMessage(...)`, tracking `lastNotifiedAt` (seeded at activation) so a stale leftover file never replays on startup. Since the extension actually shipped to users is the embedded string-constant copy in `packages/cli/src/lib/ide-extension-assets.ts` (auto-extracted and installed by `ensureIdeExtension()`), not `packages/vscode-extension/`'s own build output, that constant was hand-updated to match and `EXTENSION_VERSION` bumped `0.1.0` → `0.2.0` so existing installs upgrade. **Also found:** `packages/vscode-extension`'s own `build` script (`tsc -p tsconfig.json`) is a pre-existing no-op — `noEmit: true` from `tsconfig.base.json` is never overridden in that package's `tsconfig.json`, so it only typechecks and never emits `out/extension.js`. Not fixed here (out of scope for this feature; the embedded-constants path doesn't depend on it), but worth fixing separately since it makes the package's own `build`/`watch` scripts silently useless.

**Final settled state, after several rounds of live testing in Antigravity (see spec's "Final design (v3)" section for the full blow-by-blow):** briefly tried adding a real macOS notification banner (`execFile("osascript", ...)` from the extension, chosen based on VS Code's `state.focused` vs. our own `isTerminalFocused()`) to reach the user even when they'd left the app entirely — reverted. Root causes found via temporary debug logging (`~/.koincode/notify-debug.log`, since removed): (1) an intermediate fix mistakenly stopped gating the VS Code path on `isTerminalFocused()` at all, based on an unverified assumption that Antigravity might not forward DEC 1004 focus events — the logging proved that assumption wrong (it forwards them fine) and the ungated version was spamming a notification on every single turn; restored the gate. (2) Even correctly gated, the `osascript` banner itself never displayed — it exits cleanly with no error, but there's no "Script Editor" entry in System Settings > Notifications at all, which points at a known modern-macOS restriction on unsigned-script-triggered notifications (the standard workaround, `terminal-notifier`, isn't installed and wasn't judged worth requiring as a dependency for this). **Final shape:** VS Code-family fallback is toast-only (`vscode.window.showInformationMessage`, gated on the same `isTerminalFocused()` as the BEL path) — accepted as a known limitation that it can't reach the user once they've left the editor entirely, same as the original terminal-bell design's ceiling. `EXTENSION_VERSION` ended at `0.6.0` after the version churn from iterating against a live install.

**Fixed the pre-existing `packages/vscode-extension` build no-op flagged above.** Root cause was deeper than the missing `noEmit: false` override: `@koincode/shared`'s `package.json` exports raw `.ts` source (`"exports": {".": "./src/index.ts"}`) with no compiled output, resolvable only under Bun's own runtime/resolver — never under the plain Node.js the VS Code extension host actually runs on, so no amount of tsconfig tuning could make a real `require("@koincode/shared")` work in emitted output (confirmed by first trying `bun build` as a bundler instead of `tsc`, which "worked" but pulled in 162 modules / 0.7MB to inline three trivial path constants — rejected as bloat for a startup-activated extension). Fixed by removing the `@koincode/shared` import from `extension.ts` entirely and inlining the same three constants (`GLOBAL_CONFIG_DIR`, `IDE_CONTEXT_FILE`, `NOTIFY_REQUEST_FILE`) by hand — matching what the embedded `ide-extension-assets.ts` copy already had to do for the same reason, so the source and the shipped artifact are now structurally consistent instead of diverging only because of this constraint. With that dependency gone, `tsconfig.json` could safely add `module: "CommonJS"`, `moduleResolution: "node"`, `esModuleInterop: true`, `noEmit: false`, `outDir: "out"`, `rootDir: "src"` (plus `allowImportingTsExtensions: false`, since that option requires `noEmit`) and `bun run --cwd packages/vscode-extension build` now genuinely emits a working, dependency-free `out/extension.js`, matching what its own header comment always claimed. Removed the now-unused `@koincode/shared` workspace devDependency and ran `bun install` to sync the lockfile.

**Second follow-up, found via live testing the above in Antigravity — two more problems, both fixed:**
1. `vscode.window.showInformationMessage` is an in-editor toast only (no dock badge, no Notification Center entry), so it can never reach the user once they've tabbed away to a *different* app — the actual original complaint. A literal dock badge isn't reachable from an extension at all (owned by the host app's own Electron main process, no exposed VS Code API) — confirmed infeasible and ruled out rather than half-built.
2. The notification never fired in the first place: `ringBellIfUnfocused` gated the VS Code write on the same `isTerminalFocused()` (DEC 1004) check used for the native-terminal BEL path, which depends on the integrated terminal forwarding focus-report escapes to the shell — not guaranteed across VS Code forks, and evidently not happening in Antigravity, so `isTerminalFocused()` just stayed permanently `true` and the whole path silently no-opped.

Fix: `ringBellIfUnfocused` now skips its own focus check entirely when `TERM_PROGRAM === "vscode"` and always calls `notifyVsCode(...)`, handing the focus decision to the extension instead. `extension.ts`'s `checkNotifyRequest` now checks VS Code's own authoritative `vscode.window.state.focused` (a real API, no dependency on terminal escape forwarding): shows the quiet in-editor toast if still focused, or shells out via `execFile("osascript", ["-e", script])` for a real macOS Notification Center banner if not (falls back to the toast if `osascript` errors, e.g. non-macOS — Linux/Windows equivalents are a known gap). `escapeAppleScriptString` neutralizes `\`/`"` before interpolating the message into the AppleScript literal. `EXTENSION_VERSION` bumped `0.2.0` → `0.3.0`. Verified the nested-template-literal escaping in `ide-extension-assets.ts` (`EXTENSION_JS` embeds this as a string) actually round-trips to valid, correctly-behaving JS by evaluating it directly rather than assuming the escaping was right. **Not yet verified live** — needs a fresh CLI restart + Antigravity reload to pick up 0.3.0.

## Next Up

### Phase 2 — Local-First Pivot

- **P2-F01:** ✅ Done. Removed Clerk auth middleware, all auth routes, Polar billing middleware and billing routes. Removed `@clerk/backend` and `@polar-sh/sdk` dependencies. Removed all `userId` references from routes, schema, and generated Prisma client. Server is now unauthenticated — all endpoints accessible from localhost only. Removed `/login`, `/logout`, `/upgrade`, `/usage` CLI commands.
- **P2-F02:** ✅ Done. Swapped `@prisma/adapter-pg` → `@prisma/adapter-better-sqlite3`. Schema changed to `provider = "sqlite"`. DB path: `~/.config/koincode/data.db`. Initial migration generated at `packages/database/prisma/migrations/`. `prisma.config.ts` now computes the path dynamically (no `.env` required for DB). Server runs `prisma migrate deploy` on startup. CLI spawns the server on first run via `server-manager.ts` — health-check at `/health`, polls until ready, restarts on `ECONNREFUSED`. Server auto-shuts after 30 min of idle, CLI restarts it transparently. Port updated from 3000 → 37420 everywhere.
- **P2-F03 + P2-F04:** ✅ Done. OpenRouter integration and multi-provider key support implemented together. Keys stored in `~/.koincode/config.json` (`KoincodeGlobalConfig` type in `@koincode/shared`). CLI reads config and passes keys as env vars when spawning the server. Server model resolver checks for a direct provider key first (`ANTHROPIC_API_KEY`, `OPENAI_API_KEY`) and falls back to OpenRouter (`OPENROUTER_API_KEY`) for any model whose native key is absent. `/setup` command opens a dialog with rows for OpenRouter, Anthropic, OpenAI, and Gemini keys — navigate with ↑↓, Enter to edit, Esc to cancel/close. Keys can also be saved non-interactively: `koincode --openrouter-key sk-xxx`, `--anthropic-key`, `--openai-key`, `--gemini-key`.
- **P2-F05:** Update server port from 3000 → 37420 across server config, `API_URL` default, and CLAUDE.md.
- **P2-F06:** ✅ Done. `Memory` model added to Prisma schema (`id`, `content`, `createdAt`, `updatedAt`). Migration `20260529172100_add_memory` created and applied. CRUD routes at `/memory` (GET list, POST create, PATCH `:id`, DELETE `:id`). Chat route fetches all memories on each request and passes concatenated content to `buildSystemPrompt` as `userMemory`. System prompt injects a `# Remembered Context` section when memories exist. Memory tool calls (so the agent can manage memory itself) are deferred — see Deferred section.
- **Memory tool calls** — Expose the memory CRUD routes as agent tool calls (add, update, delete, list) so the AI can manage user memory directly during a session. Routes and DB table are already implemented; only the tool contracts and CLI-side execution handlers need to be added.
- **Text highlight IDE context (Feature 38)** — Implemented end to end, matching the spec's hybrid ambient/one-shot design: the extension captures the editor selection continuously (no attach hotkey), the status bar shows an indicator, but the selected text is injected into only the *next* message sent, then consumed/cleared — unlike `ideActiveFile`, which re-injects every turn.
  - **Extension** (`packages/vscode-extension/src/extension.ts`): new `onDidChangeTextEditorSelection` listener (ignores events from inactive panes via `e.textEditor !== vscode.window.activeTextEditor`). `activeFile` and `selection` are now tracked as in-memory module state (`currentActiveFile`/`currentSelection`) and written together via one `writeIdeContext()` call — writing just one field previously would've clobbered the other, since both live in the same `ide-context.json` file. Selected text capped at 20,000 chars (`MAX_SELECTION_CHARS`) with a truncation marker, so a huge selection can't balloon the request body. Line numbers are 1-indexed (`selection.start.line + 1`) to match the editor gutter. `deactivate()` clears both fields. Version bumped `0.6.0` → `0.7.0`; embedded copy regenerated via `bun run sync:ide-extension` (`packages/cli/src/lib/ide-extension-assets.ts`).
  - **CLI** (`packages/cli/src/hooks/use-ide-context.ts`): `IdeSelection` type (file, startLine, endLine, text) now parsed alongside `activeFile`. `getIdeSelectionForRequest()` is a **side-effecting** getter — unlike `getIdeContextForRequest()`, it clears the module-level `_selection` and notifies a `Set` of subscriber callbacks when read, so it's consumed exactly once. `useIdeContext()` subscribes to that notifier in its effect (`onConsumed => setSelection(null)`) so the status bar indicator disappears the instant `use-chat.ts` sends the selection, without waiting for the next `fs.watch` tick (the extension itself doesn't clear the file on consumption, so nothing would otherwise trigger a re-read). Hook gained `selection`, `selectionContextEnabled`, `toggleSelectionContext` — same shape as the existing `activeFile` trio. `use-chat.ts` adds `ideSelection: getIdeSelectionForRequest()` to the POST body alongside `ideActiveFile`. `status-bar.tsx` renders `N line(s) selected · file.ts:12-18` (dim, click to toggle via `toggleSelectionContext`, strikethrough when disabled) to the left of the existing `In <filename>` text — both can show simultaneously. No `input-bar.tsx` change needed: `StatusBar` already calls `useIdeContext()` itself rather than receiving it via props.
  - **Server**: `submitSchema` (`routes/chat.ts`) gained `ideSelection: { file, startLine, endLine, text } | null`, destructured alongside `ideActiveFile`. New `appendSelectionContext` (`lib/prompt-caching.ts`) mirrors `appendIdeContext`'s shape exactly — appends a `# Selected Code` block (with the code fenced) onto the last user/assistant message, never the system prompt, for the same Anthropic cache-breakpoint-preservation reason documented on `appendIdeContext`. Runs *after* `appendIdeContext` so both blocks land on the same message when both are present (file-open context and selection context are complementary, not exclusive).
  - Verified via `bun run typecheck` (clean across shared/cli/server/vscode-extension) and the vscode-extension's own `tsc -p tsconfig.json` build. **Not verified live** — no real VS Code/Antigravity session was exercised in this pass to confirm the indicator/injection round-trip end to end.
  - Spec: `context/feature-specs/38-text-highlight-ide-context.md`.

- **Revert file mutations on turn delete (Feature 39)** — Deleting the last turn (`esc esc` / `DELETE /:id/messages/last-user`) now auto-reverts any `writeFile`/`editFile` mutations it made; `shell` is out of scope (not safely revertible from a snapshot).
  - **Snapshot capture** (`packages/cli/src/lib/snapshots.ts`, used from `tools/write-file.ts` and `tools/edit-file.ts`): each writeFile/editFile call hashes its before/after content and, if under `MAX_SNAPSHOT_BYTES` (2MB), writes the "before" content as a content-addressed blob under a per-project subfolder: `~/.koincode/snapshots/<sha256(cwd)>/<sha256(content)>` — hashing `cwd` rather than using the raw path since paths aren't safe directory names, and namespacing per project so one project's blobs don't pile into a single flat pool shared with every other project ever opened. The tool's return value gains a `snapshot: { path, beforeHash, afterHash }` field, which rides into the already-persisted tool-output JSON with no extra plumbing.
  - **Revert logic** (`packages/cli/src/lib/revert-mutations.ts`): `collectMutations` pulls snapshot fields out of a message range's tool parts; `planRevert` groups by file and walks each file's mutations backward, verifying a hash chain (current on-disk hash must match the most recent tracked `afterHash`) — a mismatch (hand-edited, reformatted by something like prettier, or the blob was already swept) is flagged as a `conflict` and excluded rather than silently overwritten. `applyRevert` restores the oldest `beforeHash` content (or deletes the file, if it didn't exist before the range).
  - **Orphan sweep** (`sweepOrphanSnapshots` in `snapshots.ts`, triggered once per `useChat` mount, throttled to 24h *per project* via `<project-snapshot-dir>/.sweep-state.json`): fetches `GET /snapshots/referenced-hashes?cwd=<process.cwd()>` (new route, `packages/server/src/routes/snapshots.ts`, filters `db.message.findMany({ where: { session: { cwd } } })` via `Session.cwd`) and deletes any file under the current project's snapshot subfolder not in that set. Deliberately never calls `ensureServerRunning()` itself — forcing a server spawn just for background cleanup would add startup latency; if the server isn't already up it just skips and retries next time.
    - **Design correction, same session:** originally built as a dedicated `MessageSnapshot` Prisma model (new table, populated at both message-persistence points in `chat.ts` via `randomUUID()`-assigned ids and `extractSnapshotHashes`) so cleanup wouldn't need to scan message JSON. Reconsidered and removed: the sweep only runs once a day in a local single-user app, so a full `db.message.findMany` + JSON-parse pass is cheap enough that the extra schema migration, per-message-save inserts, and third touched package (`@koincode/database`) weren't worth it. `/referenced-hashes` now just reads matching messages' `content`, parses it, and reuses `extractSnapshotHashes` (`packages/server/src/lib/message-snapshots.ts`, kept — still useful as a pure function) directly — no schema change, back to two packages.
    - **Second correction, same session:** snapshot storage itself was then made per-project (as described above) rather than one flat global directory — same underlying reasoning (don't pool unrelated projects' data together) applied one layer down, from the DB-tracking question to the filesystem-layout question. Verified against the real local DB: `Session.cwd` scoping returned the same hash set as an unscoped full scan (524 of 670 real messages belonged to this project, all 6 referenced hashes matched), confirming the filter is correct, not just faster. The 3 snapshot blobs already written flat during earlier verification (before this change) were migrated into the new project subfolder by hand — a one-off manual fix, not a general migration path, since nothing had shipped yet.
  - **UI** (`packages/cli/src/screens/session.tsx`, new `RevertConfirmWidget` in `components/widget/`): double-escape now calls `initiateDelete()`, which collects mutations in the about-to-be-deleted range; none found → deletes immediately (unchanged behavior), some found → shows a confirm widget (same pattern as `ModeSwitchWidget`/`ApprovalWidget`) listing files to be reverted and calling out any conflicts by name before the user confirms. The confirm button's label itself is conditional — `"Delete turn (revert files)"` / `"Delete turn (partially revert files)"` / `"Delete turn (no files reverted)"` — since deleting the turn always happens regardless of conflicts, and the original flat "Delete and revert files" wording was actively wrong whenever a conflict left zero files reverted.
  - **Tool-view bug found and fixed along the way**: `EditFileDiff`/`WriteFilePreview` (`components/tool-view/edit-file.tsx`, `write-file.tsx`) rendered their diff/content preview from the tool call's *input* regardless of whether the call actually succeeded — so a failed `editFile` (e.g. `oldString` not found) still showed a green "added" diff block as if the edit landed, with the error text below it in plain unstyled red, indistinguishable from normal output. Fixed to suppress the preview entirely on error and show a `✗` in the header, matching the existing convention in `shell.tsx`.
  - Verified with a standalone script exercising the real `runWriteFile`/`runEditFile`/`collectMutations`/`planRevert`/`applyRevert` functions against real files on disk (not a reimplementation): 18 assertions covering restore, delete, multi-edit LIFO chains, external-modification conflict, and missing-snapshot-blob conflict, all passing, fully self-cleaning. `bun run typecheck` clean across all four packages. **Not verified live in-agent** — no real TTY session to drive an actual writeFile → esc-esc → confirm-dialog round trip.
  - Spec: `context/feature-specs/39-revert-mutations-on-turn-delete.md`.

## Deferred (Future Implementation)

These were scoped out and should be revisited:

- **Voice recorder bootstrap** — Re-enable the `/voice` command by shipping a pre-compiled macOS recorder binary inside the CLI package instead of compiling it at runtime. Steps: (1) compile `packages/cli/src/lib/voice-recorder.ts`'s `SWIFT_SOURCE` once for arm64 and x86_64, combine with `lipo -create` into a fat binary, commit to `packages/cli/bin/koincode-recorder`; (2) in `voice-recorder.ts`, copy from the package asset to `~/.koincode/bin/koincode-recorder` on first use instead of running `swiftc`; (3) un-comment the `/voice` command in `commands.tsx` and the warmup effect in `input-bar.tsx`. This eliminates the Xcode CLT requirement entirely and gives the same native "Terminal would like to access the microphone" popup with zero install friction.

- **`koincode install <skill>`** — Wrap npm install to pull skills published as npm packages (naming convention: `@koincode-skills/<name>`). The install command would fetch the package and extract its files into `~/.koincode/skills/<name>/`. Also consider a GitHub shorthand (`koincode install github:user/my-skill`). No custom registry needed — npm is the registry. Requires `koincode install` CLI subcommand.

- **Hooks tool call extension** — `executeHook` in `packages/shared/src/index.ts` handles `command` and `mcpTool` hook types. Still to implement: `http`, `prompt`, and `agent`.
- **Browser control: headed mode toggle** ✅ Done — `headless` flag implemented; browser can be run in headed (visible window) mode for debugging.
- **Browser control: multi-tab support** — Single page per browser session for now.
- **Browser control: mobile viewport emulation** — Set device viewport dimensions to simulate mobile.
- **Browser control: Firefox/WebKit** — Chromium only for the initial implementation.
- **Browser control: recording/replaying test scripts** — Save browser interactions as reusable test scripts.
- **Compiled binary syntax highlighting** — TreeSitter can't load in compiled binaries (WASM workers don't resolve from Bun's virtual filesystem `/$bunfs/`). Replace with a lightweight regex-based highlighter that runs in pure JS with no external files. Covers keyword/string/comment/type coloring for common languages (JS/TS/Python/Go/Rust/etc.).

## Open Questions

- None.

## Architecture Decisions

- **Always slice before sending to the model** — Any server route that sends message history to an LLM must first call `getLastBoundaryIndex(messageRecords)` and slice from that index + 1. This ensures the model only sees the current context window (post-clear or post-compact). Applied consistently in `chat.ts` (live stream), `sessions.ts` compact, and `sessions.ts` handoff. `getLastBoundaryIndex` lives in `lib/helpers.ts`.

- Bun workspaces monorepo — four packages: cli, server, database, shared.
- OpenTUI + React 19 for terminal rendering (not Ink).
- Tool calls executed client-side in the CLI; server only orchestrates the AI turn.
- Provider resolution is model-driven: the model the user selects determines which key is used. Direct provider keys (Anthropic, OpenAI, Gemini) take priority when the model maps to that provider; OpenRouter is the fallback for any model without a matching direct key.
- Config stored globally at `~/.koincode/config.json` — holds provider keys and user preferences, shared across all projects.
- Project-specific config stored at `.koincode/config.json` — holds project-level hooks and permissions.
- Hooks support both global and project scopes: global hooks apply to all projects, project hooks override global hooks for the current project.
- Message history stored as a JSON blob on the Session row rather than normalized message rows.
- PLAN mode and BUILD mode share the same session; mode is selected at session creation and controls which tools are exposed to the model.

## Session Notes

- Bun as package manager and runtime throughout (no Node/npm).
- React 19.2.4, React Router 7, Zod 4, Hono 4, AI SDK 6.
- Default model: `claude-opus-4-6` (will become an OpenRouter model in Phase 2).
- Production DB was Neon PostgreSQL — being replaced with local SQLite in Phase 2.
- Port changing from 3000 → 37420 in Phase 2 to avoid common port conflicts.
